package com.example.skillmatch;

public class singleSignOn {
    public void singleSignOn(String provider) {
        // Logic for single sign-on (SSO) with the specified provider (e.g., Google)
    }
}
